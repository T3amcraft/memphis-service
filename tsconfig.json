{
  // Nuxt generates the real configs into .nuxt on `nuxt prepare`; this root
  // file just points the editor and `npm run typecheck` at them.
  "files": [],
  "references": [
    { "path": "./.nuxt/tsconfig.app.json" },
    { "path": "./.nuxt/tsconfig.server.json" },
    { "path": "./.nuxt/tsconfig.shared.json" },
    { "path": "./.nuxt/tsconfig.node.json" }
  ]
}
